//8.0

data X [A] = X(A, A)

mod Xmod [A] for X[A]
	let f (x : X[A], f : \A -> Real) : Real
		f(x.0) + f(x.1)

let main () : String
	let x = X[Real](3.0, 1.0)
	let x =
		x.f(
			\x -> x*2.0
		)
	x.show()
