//6

let main () : Int
	let xs = [1, 2, 1, 2]
	let mut y = 0
	foreach x in iter(xs, 0)
		set y = y + x
	y



iterator iter [A] (xs : Vec[A], mut i : Int) : A
	loop
		if i >= xs.length()
			break

		yield xs.atOrPanic(i)
		set i = i + 1
